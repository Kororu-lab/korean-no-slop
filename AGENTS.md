# 저장소 작업 규칙

skill의 정본은 `skills/korean-no-slop`이다. 규칙을 바꾸면 해당하는 평가 입력과 의미 보존 사례를 함께 확인한다.

- 사용자가 지적한 구조·구성·편집 행동을 기준으로 범위를 유지한다.
- 금지 문자열 검사를 문체 품질이나 지시 이행의 완전한 검증으로 보고하지 않는다.
- 평가 실행자에게 `evals/rubric.json`이나 이전 결과를 주지 않는다.
- 평가 출력은 원본 그대로 보존하고 판정·설명은 별도 기록한다.
- 필요한 비교, 부정, 직접 인용, 부제와 도움말이 유지되는지 검토한다.
- UI·슬라이드를 실제 수정했다면 해당 형식의 렌더링 검증을 수행한다.
- 지침의 실패를 고칠 때는 관찰한 사례에 필요한 최소 규칙부터 수정한다.
- 게시 전 `python3 scripts/check_repository.py`와 기록된 결과의 검사를 실행한다.
